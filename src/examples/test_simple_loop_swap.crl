// Test simple loop with tuple unpacking

// Create array instance
arr = Array([5, 3, 8, 1, 4, 2])
print("Original array:")
arr.print()

print("\nUsing variables for indices:")
// Define indices outside the tuple swap
i = 0
j = 1
(arr.elements[i], arr.elements[j]) = (arr.elements[j], arr.elements[i])
print("After swap with variables:")
arr.print()

print("\nLet's try with a simple loop:")
// Try a single-iteration loop
for k in range(1):
    i = 1
    j = 2
    print("  Before swap:", arr.elements[i], arr.elements[j])
    (arr.elements[i], arr.elements[j]) = (arr.elements[j], arr.elements[i])
    print("  After swap:", arr.elements[i], arr.elements[j])

print("Final array:")
arr.print()