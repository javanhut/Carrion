// Test implementing bubble sort with tuple unpacking

// Create a test array
arr = [5, 3, 8, 1, 4, 2]
print("Original array:", arr)

// Implement bubble sort with tuple unpacking
print("\nImplementing bubble sort with tuple unpacking:")
n = len(arr)
for i in range(n):
    for j in range(0, n-i-1):
        if arr[j] > arr[j+1]:
            (arr[j], arr[j+1]) = (arr[j+1], arr[j])

print("Sorted array:", arr)