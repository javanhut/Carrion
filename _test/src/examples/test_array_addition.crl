// Test array addition
a = [1, 2, 3]
b = [4, 5, 6]

print("Array a: " + str(a))
print("Array b: " + str(b))

// This will fail because array addition isn't implemented yet
attempt:
    c = a + b
    print("Array a + b: " + str(c))
ensnare "unknown operator or type mismatch":
    print("Cannot add arrays yet!")
resolve:
    print("Test complete")